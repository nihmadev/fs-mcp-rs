import type { useProfileActions } from "./useProfileActions";

/** Inferred action bundle returned by the profile management hook. */
export type ReturnTypeOfProfileActions = ReturnType<typeof useProfileActions>;
